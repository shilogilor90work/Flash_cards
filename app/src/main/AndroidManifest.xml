<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flash_cards">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flash_cards">
        <activity android:name="Friends" />
        <activity android:name=".Definitions" />
        <activity android:name=".Subjects" />
        <activity android:name=".Quiz" />
        <activity android:name=".Login" />
        <activity android:name=".FinalScore"/>
        <activity android:name=".TeacherSubjects"/>
        <activity android:name=".Students"/>
        <activity android:name=".TeacherDefinitions"/>
        <activity android:name=".TeachersStudents"/>

        <activity android:name=".NotEnoughDefinitions"/>
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>